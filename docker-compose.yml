services:
  postgres:
    image: postgres:16-alpine
    container_name: buyersmarket-pg
    restart: unless-stopped
    environment:
      POSTGRES_DB: buyersmarket
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d buyersmarket"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
